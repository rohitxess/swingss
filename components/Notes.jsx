import { useAuth } from "@/context/AuthContext";

export default function Notes(){
    // const  { currentUser } = useAuth()

    return (
        <div>
            hi there from notes
        </div>
    )
}